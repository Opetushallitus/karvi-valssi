import {describe, test, expect} from 'vitest';
import * as RemoveKysely from './RemoveKysely-service';

describe('RemoveKysely', () => {
    test('it should exist', () => {
        expect(RemoveKysely).toBeTruthy();
    });
});
