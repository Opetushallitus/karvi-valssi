import {describe, test, expect} from 'vitest';
import * as updateOneKysely from './UpdateKysely-service';

describe('UpdateKysely', () => {
    test('it should exist', () => {
        expect(updateOneKysely).toBeTruthy();
    });
});
