import {test, expect, describe} from 'vitest';
import {myFunction} from './TemplateName-service';

describe('TemplateName', () => {
    test('it should exist', () => {
        expect(myFunction).toBeTruthy();
    });
});
